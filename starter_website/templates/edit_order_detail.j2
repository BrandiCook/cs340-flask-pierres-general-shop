<html>

<head>
	<title>Edit Order Detail</title>
  <link href="style.css" rel="stylesheet" type="text/css" />
</head>

<body>
  
  <div id="edit">
    <!-- form to edit data in OrderDetails-->
    <form id="editOrderDetail" action="/edit_order_detail/{{data[0].orderDetailID}}" method="post">
                <legend><strong>Edit Order Detail</strong></legend>
                <fieldset>
                    <!-- We can preset form inputs to match attributes currently stored for 'id' of person to be updated via value= and accessing our passed data tuple -->
                    <!-- Create hidden form input to store the passed 'id' of the person we are editing so we can access it easily in our 'POST' method in our app.py -->
                    <input type="hidden" value="{{data[0].orderDetailID}}" class="form-control" id="orderDetailID" name="orderDetailID" required>
                    <label for="orderID">Order ID:</label>

                    <select class="form-control" id="orderID" name="orderID" pattern="[0-9]{1,}" placeholder=""> 
                        <!-- Access our order_id tuple passed from app.py-->
                        {% for order_id in order_id %}
                              {% if order_id['orderID'] == data[0].orderID %}
                                <option value="{{ order_id['orderID'] }}" selected> {{order_id['orderID']}}</option>
                              {% else %}
                                <option value="{{ order_id['orderID'] }}"> {{order_id['orderID']}}</option> 
                              {% endif %}
                        <!-- End of this for loop -->
                        {% endfor %} 
                    </select>
                    
                    <label for="productID">Product Name:</label>

                    <select class="form-control" id="productID" name="productID" pattern="[0-9]{1,}" placeholder=""> 
                        <!-- Access our product_names tuple passed from app.py-->
                        <option value="0">&nbsp;</option>
                        {% for product_names in product_names %}
                              {% if product_names['productID'] == data[0].productID %}
                                <option value="{{ product_names['productID'] }}" selected> {{product_names['productName']}}</option>
                              {% else %}
                                <option value="{{ product_names['productID'] }}"> {{product_names['productName']}}</option> 
                              {% endif %}
                        <!-- End of this for loop -->
                        {% endfor %} 
                    </select>
                    
                </fieldset>
                <input type="submit" value="Edit Order Detail" name="Edit_Order_Detail" class="btn btn-primary" style="margin:.5rem;">
                <a href='/orders'><input class="btn" type="button" value="cancel"></a>
            </form>
  </div>
</body>
</html>